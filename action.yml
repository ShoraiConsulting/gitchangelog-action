name: 'Gitchangelog Action'
description: 'Generate gitchangelog for newly created tag and release it'
author: 'ShoraiConsulting'
inputs:
  changelog:
    description: 'Path to changelog file'
    required: false
env:
  GITHUB_TOKEN: 'Github Auth Token'
runs:
  using: 'docker'
  image: 'Dockerfile'
branding:
  color: 'purple'
  icon: 'package'
